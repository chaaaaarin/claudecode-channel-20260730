# 🎁 広告運用・代理店業務 実践プロンプト100選

今回取り上げた広告代理店2社が実際に自動化していた業務をもとに、クリエイティブ生成から経営管理まで、広告運用の現場でそのまま使える実践プロンプトを100本まとめました。

**受け取り方**: 各コードブロック右上のコピーボタンでコピーするか、このファイルをそのままダウンロードしてください。

**使い方**: Claude Code・Claude・ChatGPT等に貼り、渡す資料や状況を自分の案件に差し替えてください。

**広告以外の職種の方へ**: 同じ考え方を業種別にまとめた[業種別 業務自動化プロンプト100選](./work-prompts100.md)も用意しています。

## クリエイティブ生成・素材制作

### 1. 実写リファレンスから広告バナーを量産する

```text
前提: 実写で写真・映像を撮った商品素材を数枚アップロードした状態で使う、広告クリエイティブ担当者向けの指示。
指示: アップロードした写真をリファレンスとして、商品のディテール（色・形状・ロゴ配置）を変えずに、背景と構図だけを変えたバリエーションを5案作って。1案ごとに狙っているターゲット層も添えて。
出力形式: 各案ごとに「案名／狙い（1行）／変更点（背景・構図）」を箇条書きでまとめ、生成の意図が一覧できる表にして。
```

### 2. 物理的に不可能な演出を検討する

```text
前提: 商品の内部構造や作用を視覚的に見せたい広告クリエイティブの企画段階。
指示: この商品（サプリ／化粧品／家電など、都度指定）の効果を、実写では撮れない見せ方（体内での動き、内部構造の透過など）でアイデア出しして。実写と生成AIそれぞれで実現可能かどうかも判定して。
出力形式: アイデアごとに「見せ方／実写可否／生成AI向き度（◎○△）」の3列表で。
```

### 3. 既存クリエイティブの量産バリエーション出し

```text
前提: 1本の当たりクリエイティブがあり、そこから展開案を増やしたい状況。
指示: この当たりクリエイティブの構成要素（フック・訴求ポイント・CTA）を分解し、フック部分だけを5パターン変えた台本案を作って。訴求ポイントとCTAは変えないこと。
出力形式: 元クリエイティブの分解結果を先に箇条書きで示し、そのあとにフック違いの5パターンを番号付きリストで。
```

### 4. リファレンス画像を使ったプロンプト設計

```text
前提: 画像生成AI（Nano Banana等）に渡すプロンプトを作る場面。参考画像はすでに用意されている。
指示: 添付のリファレンス画像の構図・光の当たり方・色味を維持しつつ、被写体だけを別の商品に差し替えるための画像生成プロンプトを作って。商品のディテールを崩さない指示を明記すること。
出力形式: 生成プロンプト本文をそのまま貼れる形で1つ出し、あわせて「崩れやすいポイントと対策」を3行で添えて。
```

### 5. 実写で撮った方がいいものを判定する

```text
前提: クリエイティブ制作の初期段階で、AI生成と実写のどちらで作るか判断したい。
指示: これから作る素材リスト（都度渡す）について、それぞれ「AI生成向き」か「実写の方がいい」かを判定して。商品ディテールの正確性が重要なものは実写寄りに倒す判断基準で。
出力形式: 素材名／判定／理由（1行）の3列表。
```

### 6. CG風の演出案を安く作る

```text
前提: 従来は外部発注していたCG演出を、生成AIでのコスト圧縮に置き換えたい。
指示: 従来CGで発注していた演出内容（都度渡す）を、生成AIで代替できないか検討して。代替できる場合はそのままのプロンプト案を、できない場合は理由と次善策を出して。
出力形式: 「代替可否／プロンプト案 or 次善策」の2段構成で、判断の根拠も1行添える。
```

### 7. 広告動画の背景だけ差し替える

```text
前提: すでに撮った動画素材があり、コンセプトに合わせて背景の雰囲気だけ変えたい。
指示: 主役（人物・商品）の動きや色味は変えず、背景だけを「白背景／黒背景／暖色系の生活感ある背景」の3パターンに差し替える生成の指示文を作って。
出力形式: 3パターンそれぞれの指示文を分けて出し、どのコンセプトに合うかを1行で添える。
```

### 8. 複数商品のクリエイティブを一括量産する

```text
前提: 同じフォーマットで扱う商品数が多い（10点以上）状況。
指示: 共通のバナーフォーマット（構図・文字配置）を1つ決め、渡した商品リストの分だけ、商品名・価格・訴求文だけを差し替えたバリエーションを作る手順を設計して。
出力形式: 手順を番号リストで示し、最後に「差し替えが必要な要素一覧」をチェックリスト形式で。
```

### 9. インタメ系エンタメ演出のアイデア出し

```text
前提: 若年層向けにエンタメ性の強いクリエイティブを企画している。
指示: この商品カテゴリ（都度指定）で、SNSでバズりやすいエンタメ演出（誇張・コミカルな展開等）のアイデアを10本出して。景表法・プラットフォーム規約に抵触しない範囲で。
出力形式: アイデアごとに「概要／狙う反応（笑い・驚き等）」の2列。
```

### 10. クリエイティブの色味・トーンを統一する

```text
前提: 複数人が別々に作った素材を、1つのブランドトーンに揃えたい。
指示: 渡す複数のクリエイティブ（色味・雰囲気がバラバラ）を、指定のブランドカラー・トーンに揃えるための修正指示を、素材ごとに作って。
出力形式: 素材名／現状の課題／修正指示（1〜2行）の3列表。
```

### 11. 代替素材のプロンプトを即興で作る

```text
前提: 用意する予定だった素材が急遽使えなくなり、生成AIで代替する必要がある。
指示: 用意する予定だった内容（構図・被写体・シーン）を伝えるので、それを再現する生成プロンプトをその場で作って。ディテールが重要な箇所は「参考資料が必要」と明記して。
出力形式: 生成プロンプト本文＋「参考資料なしで再現できる自信度（高中低）」を添えて。
```

### 12. ブランドガイドラインに沿った素材チェック

```text
前提: 生成した素材が、ブランドのトンマナ規定に沿っているか確認したい。
指示: 渡すブランドガイドライン（色・フォント・NG表現）と、生成済みの素材の説明を照合し、逸脱している箇所を指摘して。
出力形式: 逸脱箇所／ガイドラインのどの項目に反するか／修正案、の3列表。
```

### 13. Before/After訴求のビジュアル台本を作る

```text
前提: 商品の効果を視覚的に伝えるBefore/After構成のクリエイティブを作りたい。
指示: この商品（都度指定）のBefore/After訴求を、誇張しすぎない範囲でビジュアル台本にして。効果を保証するような断定表現は避けること。
出力形式: Before描写／After描写／ナレーション案、の3段構成。
```

### 14. 素材のサイズ・比率違い展開を洗い出す

```text
前提: 1本の素材を複数の配信面（Feed/Story/Reels等）に展開したい。
指示: 元の素材（構図情報を渡す）を、Feed用（1:1）・Story用（9:16）・横型（16:9）にそれぞれ再構成するときに、削れる・足りなくなる要素を洗い出して。
出力形式: 配信面ごとに「必要な調整点」を箇条書きで。
```

### 15. ペルソナ別に訴求ポイントを変えた素材案

```text
前提: 同じ商品を異なるターゲット層に向けて展開したい。
指示: この商品（都度指定）について、渡す3つのペルソナそれぞれに刺さる訴求ポイントとビジュアルの方向性を提案して。
出力形式: ペルソナ名／刺さる訴求／ビジュアルの方向性、の3列表。
```

### 16. 生成AIで作った素材のクレジット消費を見積もる

```text
前提: 生成AIツールのクレジット（利用枠）を管理しながら制作したい。
指示: これから作りたい素材リスト（点数・複雑さ）を渡すので、試行錯誤を含めた想定クレジット消費量と、消費を抑えるための工夫を提案して。
出力形式: 素材ごとの想定消費量（相対的な多寡）と、節約のコツを箇条書きで。
```

### 17. 参考動画のトーン分析から自社案を作る

```text
前提: 他社の反響が良かった広告動画を参考にしたいが、模倣にならないようにしたい。
指示: 参考動画の構成（テンポ・カット割り・訴求の順番）を要素分解し、それを踏まえた「自社商品向けの別企画」を1本考えて。表現やセリフは真似しないこと。
出力形式: 参考動画の構成分析（箇条書き）→ 自社案の台本骨子、の2段構成。
```

### 18. クリエイティブのA/Bテスト設計

```text
前提: 複数の訴求パターンを比較検証したい。
指示: この商品（都度指定）について、フック・訴求・CTAのいずれか1要素だけを変えたA/Bパターンを3セット設計して。検証したい仮説も添えて。
出力形式: パターン名／変更箇所／検証したい仮説、の3列表。
```

### 19. 季節・イベントに合わせた素材展開案

```text
前提: 年間の販促カレンダーに沿ってクリエイティブを企画したい。
指示: 直近3ヶ月のイベント（都度指定、例: 季節の変わり目・セール時期）に合わせて、この商品のクリエイティブ企画案を月ごとに1本ずつ出して。
出力形式: 月／イベント／企画概要、の3列表。
```

### 20. 縦型ショート動画の冒頭2秒フック案

```text
前提: TikTok/Reels/Shorts向けの縦型動画で、離脱の多い冒頭を強化したい。
指示: この商品（都度指定）の縦型ショート動画について、最初の2秒で止めさせるフック案を10本出して。誇張表現・虚偽にならない範囲で。
出力形式: フック案を番号リストで、それぞれ狙いを1行添える。
```

## 広告運用・入稿・レポート

### 21. 複数アカウントへの一括入稿設計

```text
前提: 同じクリエイティブを複数の広告アカウントに入稿する必要がある。
指示: このクリエイティブ（本数）を、渡すアカウントリスト（数・媒体）に一括入稿する手順を設計して。アカウントごとに異なる設定（年齢・地域・配信面）がある場合の扱い方も含めて。
出力形式: 手順を番号リストで、アカウントごとの差分は表で整理して。
```

### 22. 入稿前のターゲティング設計チェック

```text
前提: 新規キャンペーンの入稿直前に設定ミスがないか確認したい。
指示: この広告のターゲティング設定（年齢・地域・興味関心・配信面）を渡すので、ちぐはぐな点や、商品と噛み合っていない箇所を指摘して。
出力形式: 設定項目／懸念点／修正案、の3列表。
```

### 23. 日次レポートの自動要約フォーマットを作る

```text
前提: 毎朝の数値確認を、AIによる要約通知に置き換えたい。
指示: 渡す広告管理画面のスクリーンショットまたは数値データから、CPA・CTR・消化金額の要点を3行で要約するフォーマットを設計して。異常値（前日比◯%以上の変化）は強調表示するルールも含めて。
出力形式: 要約テンプレート本文（3行）＋異常値の判定基準、の2段構成。
```

### 24. 予算オーバー（防発）の早期検知ルール

```text
前提: 予算超過に気づくのが遅れて赤字化するのを防ぎたい。
指示: この広告アカウントの日予算と現在の消化ペースを渡すので、このペースで進むと何時ごろ予算超過するかを試算し、対応が必要なタイミングを教えて。
出力形式: 試算結果（時刻の目安）と、推奨アクション（配信停止／予算調整等）を1〜2行で。
```

### 25. 配信結果のPDCA判断を支援する

```text
前提: 配信中の広告の成果が微妙で、次のアクションを決めたい。
指示: この広告の直近の数値推移（CPA・CTR・フリークエンシー）を渡すので、「クリエイティブを変える」「ターゲットを絞る」「配信を止める」のどれが適切か、判断根拠つきで提案して。
出力形式: 判断（1つに絞る）／根拠（数値ベース）／次にやること、の3項目。
```

### 26. 複数媒体の成果を横並びで比較する

```text
前提: Google・Meta・LINEヤフー・TikTok等、複数媒体に配信している状況。
指示: 渡す媒体別の数値（CPA・CVR・消化金額）を横並びで比較し、どの媒体に予算を寄せるべきか提案して。媒体特性（ユーザー層の違い）も踏まえること。
出力形式: 媒体比較表＋提案（1〜2行）。
```

### 27. 入稿ミスの事前チェックリストを作る

```text
前提: 予算を預かる立場として、入稿ミスによる過剰配信を防ぎたい。
指示: 入稿前に確認すべき項目（予算・期間・ターゲティング・クリエイティブ枚数など）を、渡す過去のミス事例をもとにチェックリスト化して。
出力形式: □のチェック項目を箇条書きで、重要度の高いものには⚠️を付けて。
```

### 28. 週次の予算配分を再設計する

```text
前提: 週の途中で成果の良し悪しに応じて予算配分を見直したい。
指示: 渡す週次の広告アカウント別成果（CPA・消化金額）をもとに、来週の予算配分案を作って。成果の良いものに寄せつつ、テスト予算も一定確保する方針で。
出力形式: アカウント名／今週CPA／来週の配分案（増減）、の3列表。
```

### 29. クリエイティブ疲弊（フリークエンシー上昇）の検知

```text
前提: 同じクリエイティブを配信し続けて反応が落ちてきていないか確認したい。
指示: 渡すフリークエンシーとCTRの推移から、クリエイティブが疲弊しているサインが出ているか判定し、出ていれば差し替えのタイミングを提案して。
出力形式: 判定（疲弊あり／なし）／根拠／推奨アクション。
```

### 30. レポートを非エンジニアにも分かる言葉で要約する

```text
前提: クライアント向けに、専門用語を使わない報告資料を作りたい。
指示: この広告レポート（数値・専門用語込み）を、広告に詳しくないクライアント担当者にも伝わる言葉に言い換えて要約して。
出力形式: 元の数値は残しつつ、それぞれに平易な一言解説を添える。
```

### 31. 配信先の絞り込み判断（PC/モバイル・配置）

```text
前提: 配信先ごとの成果に差が出てきて絞り込みを検討したい。
指示: 渡す配置別（PC/モバイル、フィード/ストーリーズ等）の成果データから、どの配置を止めるべきか、根拠つきで提案して。
出力形式: 配置ごとの成果一覧表＋提案（止める/続ける）。
```

### 32. 複数クリエイティブの入稿順序を設計する

```text
前提: 複数本のクリエイティブを、テスト目的で段階的に入稿したい。
指示: この本数のクリエイティブを、いきなり全部ではなく段階的にテストする入稿スケジュールを設計して。学習期間や予算配分も含めて。
出力形式: 週ごとの入稿スケジュール表。
```

### 33. 赤字案件の原因切り分け

```text
前提: 配信しているが赤字（売れていない）状態の案件がある。
指示: 渡す数値（CPA・CVR・LP直帰率）から、赤字の原因が「クリエイティブ」「ターゲティング」「LP」のどこにありそうか切り分けて。
出力形式: 疑わしい箇所（優先順位つき）／根拠、の2列。
```

### 34. 複数アカウントの異常値を一括スキャンする

```text
前提: 管理しているアカウント数が多く、目視での異常検知が追いつかない。
指示: 渡す複数アカウントの数値一覧から、前日比で大きく変動している項目（予算消化・CPA・CTR）だけを抽出して報告して。
出力形式: アカウント名／異常項目／変動幅、の3列表。
```

### 35. 入稿後のダブルチェック指示文

```text
前提: 入稿ミスによるクライアント信頼低下を防ぎたい。
指示: 入稿が完了した広告設定（予算・期間・ターゲット・クリエイティブ）の内容を渡すので、意図した内容と一致しているか、想定される訴求ターゲットとズレがないかを確認して。
出力形式: 確認項目ごとに「OK／要確認」の判定と理由。
```

### 36. 成果が良かった要因の言語化

```text
前提: 好調な案件の勝ち筋を、他の案件に横展開したい。
指示: この好調な案件（数値・クリエイティブ概要）について、なぜ成果が良かったのか要因を分解して。他の案件に転用できそうな要素も指摘して。
出力形式: 要因分解（箇条書き）→ 転用できそうな要素（1〜2点）。
```

### 37. 媒体別のクリエイティブサイズ要件を整理する

```text
前提: 複数媒体に同じ企画を展開するとき、サイズ・尺の違いで手戻りが起きやすい。
指示: 渡す配信予定の媒体リストごとに、必要なクリエイティブのサイズ・尺・ファイル形式の要件を整理して。
出力形式: 媒体名／サイズ／尺／形式、の4列表。
```

### 38. 成果報告のスライド構成案を作る

```text
前提: クライアント向け月次報告のスライドを効率よく作りたい。
指示: 渡す月次数値サマリーをもとに、クライアント報告用スライドの構成案（結論→数値→要因→来月アクション）を作って。
出力形式: スライドごとのタイトルと入れる内容を箇条書きで。
```

### 39. 配信停止判断のための損益分岐点計算

```text
前提: 案件の継続可否を、感覚ではなく数字で判断したい。
指示: 渡す商品の利益率と現在のCPAから、損益分岐となるCPAラインを計算し、現状がそれを超えているか判定して。
出力形式: 損益分岐CPA／現状CPA／判定（継続可否）、の3項目。
```

### 40. 入稿作業のログを引き継ぎ用に残す

```text
前提: 担当者交代時の引き継ぎで情報が漏れやすい。
指示: 今日行った入稿・設定変更の内容を、次の担当者が読んでも分かる引き継ぎメモの形式でまとめて。
出力形式: 日付／変更内容／理由／次にやるべきこと、の4項目。
```

## レギュレーションチェック・審査対応

### 41. クライアント別NGワードリストとの照合

```text
前提: クライアントごとに使ってはいけない表現のルールが決まっている。
指示: 渡す広告原稿を、渡すNGワードリスト（このクライアント固有のルール）と照合し、該当箇所を指摘して。指摘には該当ルールの番号も添えて。
出力形式: 該当箇所（原文引用）／該当ルール／修正案、の3列表。
```

### 42. 薬機法・景表法に抵触しそうな表現の検出

```text
前提: 健康食品・化粧品等、規制の厳しいジャンルの広告原稿をチェックしたい。
指示: この広告原稿について、効果を保証する断定表現や、薬機法・景表法に抵触しやすい言い回しがないか洗い出して。断定はできないので「疑わしい箇所」として報告する形にして。
出力形式: 該当箇所／懸念理由／言い換え案、の3列表。
```

### 43. 会社独自の表記ルールチェック（大文字・社名表記等）

```text
前提: 社名やブランド名の表記ゆれを防ぎたい。
指示: この原稿に含まれる会社名・ブランド名・製品名の表記が、渡す正しい表記ルールと一致しているか確認して。
出力形式: 誤表記箇所／正しい表記、の2列表。
```

### 44. 差し戻し理由の分析と再発防止

```text
前提: 何度も同じ理由で審査に差し戻されている状況を改善したい。
指示: 渡す過去の差し戻し履歴（理由の一覧）を分析し、頻出する差し戻し理由のパターンを整理して。今後のチェック項目に加えるべきものを提案して。
出力形式: 頻出パターン（頻度順）／今後のチェック項目案。
```

### 45. 表現の言い換えバリエーションを出す

```text
前提: NGと判定された表現を、意図を保ったまま安全な表現に置き換えたい。
指示: このNG表現を、伝えたい意図（都度指定）は保ったまま、規約に抵触しない言い換え案を3パターン出して。
出力形式: 言い換え案を3つ、それぞれのニュアンスの違いを1行で。
```

### 46. 組織用語の言い換えルールチェック

```text
前提: 社内で「下の人間」等、社外向けに不適切な表現を使わないルールがある。
指示: この原稿・資料の中に、社内向けの言い回しがそのまま社外向け表現として残っていないか確認して。
出力形式: 該当箇所／推奨する言い換え、の2列表。
```

### 47. 新規クライアントのレギュレーションDB雛形を作る

```text
前提: 新しいクライアントを担当することになり、審査ルールをこれから整備したい。
指示: このクライアントから受け取ったブランドガイドライン（都度渡す）を、チェックしやすいNGワード・NG表現のリスト形式に整理して。
出力形式: カテゴリ（表現・数値・比較表現等）ごとにNG項目を箇条書きで。
```

### 48. 複数クライアントのルールを混同しないための確認

```text
前提: 複数クライアントを掛け持ちしており、ルールの取り違えが起きやすい。
指示: この原稿がどのクライアント向けかを踏まえ、他のクライアントのルールが誤って適用されていないか確認して。
出力形式: 確認結果（問題なし／要修正）と該当箇所。
```

### 49. 誇大広告になっていないかのセルフチェック

```text
前提: 訴求を強めたい気持ちと規約遵守のバランスを取りたい。
指示: この訴求文について、実際のデータや根拠と比べて誇張になっていないか確認して。根拠が弱い場合は「弱め表現」への言い換え案も出して。
出力形式: 訴求文／根拠の有無／言い換え案（必要な場合）、の3列表。
```

### 50. 比較表現（業界一位・最安等）の根拠チェック

```text
前提: 比較優位を示す表現を使いたいが、根拠が必要な規約がある。
指示: この比較表現（業界一位・最安・業界初等）について、根拠として何が必要かを整理し、渡す手元資料でその根拠を満たせているか判定して。
出力形式: 表現／必要な根拠／充足状況（○×）、の3列表。
```

### 51. 審査提出前の最終読み合わせ指示文

```text
前提: 審査に出す直前の最終チェック工程を効率化したい。
指示: この原稿一式（渡す）を、これまで指摘されたことのある観点（クライアント固有ルール・薬機法・景表法・表記ルール）で総合的にチェックして、提出可否を判定して。
出力形式: 総合判定（提出可／要修正）／観点ごとの結果一覧。
```

### 52. 動画クリエイティブのテロップ表現チェック

```text
前提: 動画内のテロップにも文字ベースの審査ルールが適用される。
指示: この動画のテロップ文字起こし（渡す）を、通常の広告原稿と同じ基準でチェックして。
出力形式: 該当箇所（該当テロップ）／懸念点／修正案、の3列表。
```

### 53. レギュレーション更新時の差分チェック

```text
前提: クライアントからルールの一部変更連絡があった。
指示: 旧ルールと新ルール（両方渡す）の差分を洗い出し、既存の稼働中クリエイティブの中に新ルールで引っかかるものがないか確認する手順を作って。
出力形式: 差分一覧（旧→新）／既存クリエイティブへの影響有無の確認手順。
```

### 54. 表現チェックの精度を上げるフィードバックループ

```text
前提: 差し戻されたものを次回から検出できるようにしたい。
指示: 今回審査で新たにNGと分かった表現（都度渡す）を、今後のチェック用データベースに追加する形式でまとめて。
出力形式: 「NG表現／NGと判断された理由／今後の判定ルールへの追加文」の3点セット。
```

## LP分析・記事/コンテンツ生成

### 55. 商品LPから記事のターゲット・悩みを抽出する

```text
前提: 記事（KLP）を作る前段階で、LPの情報を整理したい。
指示: このLPのURL（または内容）から、ターゲット層・抱えている悩み・欲しい未来（ベネフィット）を抽出して。
出力形式: ターゲット／悩み／欲しい未来、の3項目を箇条書きで。
```

### 56. ストーリーフォーミュラで記事の骨子を作る

```text
前提: LP分析の結果をもとに、記事本文の構成を組み立てたい。
指示: さっき抽出したターゲット・悩み・欲しい未来をもとに、共感→気づき→解決策提示→商品紹介の流れで記事の骨子を作って。
出力形式: 各パートの見出し案と、そこで書く内容の要点（2〜3行）。
```

### 57. 記事を大量パターン生成して選別する

```text
前提: 1つのLPから複数の切り口で記事を量産し、良いものを選びたい。
指示: このLPの情報をもとに、異なる切り口（体験談風／比較検討風／専門家解説風など）の記事骨子を5パターン作って。
出力形式: パターン名／切り口の説明／向いていそうな読者層、の3列表。
```

### 58. 記事内の画像配置プランを作る

```text
前提: 記事の文章と画像を交互に配置してテンポよく読ませたい。
指示: この記事本文（渡す）を読んで、どこに画像を入れると理解が深まるかを判定し、各箇所で入れるべき画像の内容（説明図・使用シーン等）を指定して。
出力形式: 挿入位置（見出し名）／画像の内容案、の2列表。
```

### 59. 記事のSEOタイトル・見出し案を作る

```text
前提: 検索流入も狙いたい記事を書いている。
指示: この記事本文の内容から、検索されやすいタイトル案を5本と、見出し（H2）構成案を作って。誇張・薬機法に抵触する語は避けること。
出力形式: タイトル案5本（箇条書き）＋見出し構成（番号リスト）。
```

### 60. 記事のトーンをブランドに合わせて調整する

```text
前提: 記事の文体がブランドイメージと合っているか確認したい。
指示: この記事本文を、渡すブランドトーン（例: 親しみやすい／専門的で信頼感）に合わせて調整して。
出力形式: 調整後の記事本文全体。
```

### 61. LP直帰率が高い箇所を推測する

```text
前提: LPの数値（直帰率・滞在時間）から改善点を探りたい。
指示: このLPの構成（セクション順）と、渡す数値データ（各セクションの離脱ポイントが分かるもの）から、離脱が起きていそうな箇所を推測して。
出力形式: 該当セクション／推測される離脱理由／改善案、の3列表。
```

### 62. 記事とLPの内容の整合性チェック

```text
前提: 記事からLPへ遷移したときに、内容の齟齬で離脱されるのを防ぎたい。
指示: この記事とLP（両方渡す）を読み比べて、訴求内容や約束していることにズレがないか確認して。
出力形式: ズレている箇所／記事側の記載／LP側の記載、の3列表。
```

### 63. 記事の量産におけるノウハウの言語化

```text
前提: 当たり記事を書いてきた経験を、AIが再現できる形で残したい。
指示: これまで当たった記事（都度渡す複数本）に共通する型（構成・言い回し・展開のパターン）を分析して、今後の記事生成に使えるルールとして言語化して。
出力形式: 共通する型の要素を箇条書きで、それぞれ具体例つきで。
```

### 64. 既存記事のリライト・鮮度アップ

```text
前提: 過去に書いた記事の情報が古くなってきている。
指示: この既存記事を、最新の情報（渡す最新データ・トレンド）を反映してリライトして。元の構成・トーンは維持すること。
出力形式: リライト後の記事本文全体。
```

### 65. 複数商品共通で使える記事テンプレートを作る

```text
前提: 同じジャンルの商品を今後何本も扱う予定がある。
指示: このジャンル（都度指定）の商品記事に共通して使える構成テンプレートを作って。商品ごとに差し替える箇所を明示すること。
出力形式: テンプレート本文（差し替え箇所は【】で明示）。
```

### 66. 記事のCTA（行動喚起）文言を複数出す

```text
前提: 記事の最後で商品ページへの遷移率を上げたい。
指示: この記事の内容を踏まえたCTA文言を5パターン出して。押し付けがましくなく、記事の流れから自然に読める言い回しで。
出力形式: CTA案5本（箇条書き）。
```

### 67. ユーザーの声・体験談パートの構成案

```text
前提: 記事内に信頼感を高めるユーザーの声セクションを入れたい。
指示: この商品カテゴリで、記事に入れるユーザーの声セクションの構成案（何人分、どんな属性、どんな悩み→どう変わったか）を作って。実在しない体験談を捏造しないよう、構成のみに留めること。
出力形式: 構成案（人物属性・語る内容の骨子）を3パターン。
```

### 68. 記事の読了率を上げる導入部の改善

```text
前提: 記事冒頭での離脱を減らしたい。
指示: この記事の導入部（最初の3〜5行）を、読者の悩みへの共感から始まる形に書き直して。誇張表現は避けること。
出力形式: 書き直し後の導入部を3パターン。
```

### 69. 記事作成の待ち時間中にできる次の作業を洗い出す

```text
前提: 記事生成に数十分かかる間、手が空く時間をどう使うか考えたい。
指示: 記事生成を待っている間に並行して進められる、広告運用側の別タスクを洗い出して。優先度順に並べて。
出力形式: タスク名／優先度／所要目安、の3列表。
```

### 70. 記事のファクトチェック観点リストを作る

```text
前提: AI生成記事に事実誤認が混ざっていないか確認したい。
指示: この記事本文について、数値・効果・比較表現のうち、根拠の裏取りが必要な箇所を洗い出して。
出力形式: 該当箇所（引用）／確認すべき内容、の2列表。
```

### 71. 記事のカテゴリ別パフォーマンス比較

```text
前提: どの切り口の記事が成果につながりやすいか把握したい。
指示: 渡す記事カテゴリ別の成果データ（CTR・遷移率）から、次に量産すべき切り口を提案して。
出力形式: カテゴリ別成果一覧表＋提案（1〜2行）。
```

### 72. 記事のタイトルABテスト案を作る

```text
前提: 同じ記事本文で、タイトルの効果検証をしたい。
指示: この記事本文に対して、異なる訴求角度のタイトルを3案作り、それぞれどんな読者に刺さりそうか添えて。
出力形式: タイトル案／想定読者／訴求角度、の3列表。
```

### 73. LPと記事のリンク構造を整理する

```text
前提: 複数の記事・LPが増えてきて、どこからどこに遷移させるか整理したい。
指示: 渡す記事・LPのリストから、どの記事からどのLPに遷移させるのが最適か、内容の一致度をもとに提案して。
出力形式: 記事名／推奨遷移先LP／理由（1行）、の3列表。
```

### 74. 記事量産の品質を落とさないチェック体制案

```text
前提: 生成本数を増やすほど品質のばらつきが心配になる。
指示: 記事を大量生成する運用において、最低限守るべき品質基準と、人によるレビューが必要な箇所を切り分けて設計して。
出力形式: 品質基準（箇条書き）／AI任せでよい部分／人のレビューが必須な部分。
```

## SNS企画・投稿運用

### 75. 月間投稿企画をトレンドと過去実績から作る

```text
前提: 毎月の投稿企画を、トレンド調査と過去の反応データを踏まえて作りたい。
指示: 渡す直近のトレンド情報と過去投稿のインサイトデータから、来月の投稿企画を10本作って。それぞれ狙う反応（保存・シェア・コメント等）も添えて。
出力形式: 企画タイトル／狙う反応／概要（1〜2行）、の3列表を10本分。
```

### 76. カルーセル投稿の構成案を作る

```text
前提: 横スライド式のカルーセル投稿を作りたい。
指示: このテーマ（都度指定）で、カルーセル投稿（1枚目タイトル→中身複数枚→まとめ）の構成案を作って。各スライドの見出しと中の文章要点を含めて。
出力形式: スライド番号／見出し／本文要点、の3列表。
```

### 77. ミーティング議事録から次月の企画のヒントを拾う

```text
前提: 毎月のミーティングで出た話を企画に反映したい。
指示: この議事録（渡す）から、投稿企画のヒントになりそうな話題・要望を抽出して。
出力形式: 話題／企画化するとしたらどんな投稿になるか、の2列表。
```

### 78. キャプション（本文）を複数トーンで作る

```text
前提: 投稿の画像・カルーセルは決まっていて、キャプションだけ複数案欲しい。
指示: このカルーセルの内容（渡す）に対して、キャプションを「共感重視」「情報重視」「エンタメ寄り」の3トーンで作って。
出力形式: トーン名／キャプション本文、の対で3セット。
```

### 79. ブランドチェック込みの投稿最終確認

```text
前提: 投稿前にブランドのトンマナ・NG表現を確認したい。
指示: この投稿内容（画像説明＋キャプション）を、渡すブランドガイドラインと照合し、修正すべき点があれば指摘して。
出力形式: 確認項目ごとの判定（OK／要修正）と修正案。
```

### 80. 投稿の差し戻し理由から改善指示を出す

```text
前提: 承認者から修正依頼が返ってきた企画を直したい。
指示: この差し戻しコメント（渡す）をもとに、企画のどこをどう直せばいいか、具体的な修正指示に変換して。
出力形式: 差し戻しコメント／対応する修正指示、の対応表。
```

### 81. 週次でAI活用事例を共有するテンプレート

```text
前提: 社内でAI活用の知見を共有する文化を作りたい。
指示: 今週試したAI活用の内容（都度渡す）を、他メンバーが読んで真似しやすい共有フォーマットにまとめて。
出力形式: 【やったこと】【使ったツール・プロンプト】【結果・気づき】の3項目。
```

### 82. 複数アカウント分の投稿企画を一括生成する

```text
前提: 運用するSNSアカウントが複数あり、まとめて企画を立てたい。
指示: 渡すアカウントリスト（それぞれのテーマ・ターゲット）ごとに、来月の投稿企画をそれぞれ5本ずつ作って。
出力形式: アカウント名ごとに見出しを分け、それぞれ企画タイトルを箇条書きで。
```

### 83. インサイトデータから伸びる投稿の傾向を分析する

```text
前提: 過去投稿のインプレッション・保存数データがある。
指示: 渡す過去投稿のインサイトデータから、反応が良かった投稿に共通する要素（テーマ・投稿時間・フォーマット）を分析して。
出力形式: 共通要素（箇条書き）と、次の企画に活かす提案。
```

### 84. 投稿スケジュールのカレンダー案を作る

```text
前提: 決まった企画を、投稿日にどう割り振るか決めたい。
指示: この企画リスト（渡す）を、渡す投稿カレンダーの空き枠に、テーマが偏らないよう割り振って。
出力形式: 日付／投稿テーマ、の表。
```

### 85. 投稿文の文字数・プラットフォーム制約チェック

```text
前提: プラットフォームごとに文字数やハッシュタグ数の制約が違う。
指示: この投稿文（渡す）が、指定するプラットフォームの文字数制約に収まっているか確認し、超えていれば削る案を出して。
出力形式: 判定（収まる／超過）／超過している場合の削り案。
```

### 86. フォロワーの疑問・コメントから次の企画を作る

```text
前提: 過去投稿についたコメントを企画のヒントにしたい。
指示: この過去投稿へのコメント一覧（渡す）から、多く見られる疑問・要望を抽出し、それに応える投稿企画を作って。
出力形式: よくある疑問／それに応える企画案、の対応表。
```

### 87. ハッシュタグ選定を目的別に整理する

```text
前提: 投稿の目的（認知拡大／保存狙い等）でハッシュタグ戦略を変えたい。
指示: この投稿の目的（都度指定）に合わせて、使うべきハッシュタグの種類（ビッグワード／ニッチワード等）とバランスを提案して。
出力形式: ハッシュタグの種類／狙い／推奨個数、の3列表。
```

### 88. 投稿データベースの蓄積フォーマットを設計する

```text
前提: 過去の投稿を、あとでAIが参照しやすい形で蓄積したい。
指示: 投稿ごとに記録しておくべき項目（テーマ・成果数値・採用不採用の理由等）を設計して。
出力形式: 記録項目とその説明を箇条書きで。
```

### 89. 不採用になった企画の再利用案

```text
前提: 一度ボツになった企画を、別の切り口で活かしたい。
指示: このボツ企画（内容と不採用理由を渡す）を、不採用理由を踏まえて別の切り口に作り替えて。
出力形式: 元の企画／不採用理由／作り替え後の企画。
```

## 経営管理・ダッシュボード運用

### 90. 既存ダッシュボードを自社版に再現するための質問リスト

```text
前提: 参考にしたいダッシュボード（他社事例やテンプレート）のリンクがある状態。
指示: このダッシュボードを自社版として再現したい。再現に必要な情報（プロジェクトの単位・タスクのルール・見たい指標）を洗い出す質問リストを作って。
出力形式: 質問項目を番号リストで、それぞれ「なぜその情報が必要か」を1行添える。
```

### 91. プロジェクト・タスク管理ダッシュボードの設計

```text
前提: 会社全体の進行案件を1箇所で管理したい。
指示: 渡す組織の案件の種類・規模感をもとに、プロジェクト単位・タスク単位で管理すべき項目（担当者・期限・進捗状況等）を設計して。
出力形式: 管理項目一覧（表形式）。
```

### 92. 未完了・機嫌切れタスクの検知ルール

```text
前提: 期限切れタスクが放置されがちで、把握が遅れる。
指示: タスクの期限データをもとに、機嫌切れ（期限超過）を検知し、優先度順に並べて報告するルールを設計して。
出力形式: 検知ルール（条件）／通知する情報（担当者・遅延日数等）。
```

### 93. マーケティング・セールス・CRMダッシュボードの連携設計

```text
前提: 複数のダッシュボードがバラバラで、全体像が見えにくい。
指示: マーケティング（流入経路）・セールス（成約チャネル）・CRM（顧客対応履歴）の3つのダッシュボードを、どの項目で紐づけるべきか設計して。
出力形式: 紐づけキーとなる項目／それぞれのダッシュボードで見たい指標、の一覧。
```

### 94. 週次のKPI管理シートを作る

```text
前提: 会社としてのKPI進捗を週次で追いたい。
指示: 渡す事業のKPI項目をもとに、週次で追うべき指標と、目標に対する達成率の見せ方を設計して。
出力形式: KPI項目／週次で見る数値／表示形式（達成率・推移グラフ等）。
```

### 95. 経営者が見たい数値を言語化するヒアリング設計

```text
前提: 「どんな数値を見たいか」を経営者本人からうまく引き出したい。
指示: 経営者に「見たい数値」をヒアリングするための質問リストを作って。プロジェクトの遅延・マーケティングの数字・タスクの紐付き等、幅広く聞き出せる形で。
出力形式: 質問項目を番号リストで。
```

### 96. AIエージェントに任せる管理業務の切り分け

```text
前提: 管理業務のうち、AIに任せられる部分と人が判断すべき部分を分けたい。
指示: この管理業務（都度渡す一覧）について、AIエージェントが自動処理できるもの、人の最終判断が必要なものを切り分けて。
出力形式: 業務名／AI任せ可否／理由、の3列表。
```

### 97. Slack通知の設計（見対応件数等）

```text
前提: 管理ダッシュボードの異常をSlackに自動通知したい。
指示: このダッシュボードの数値（都度指定）について、どんな条件でSlackに通知すべきか、通知の文面例つきで設計して。
出力形式: 通知条件／通知文面例、の対応表。
```

### 98. ダッシュボード複製時の設計思想を引き継ぐドキュメント

```text
前提: ダッシュボードを別チームにも展開したい。
指示: 今回作ったダッシュボードの設計思想（なぜこの項目を見るのか）を、別チームが読んでも理解できるドキュメントにまとめて。
出力形式: 【背景】【見ている指標とその理由】【運用上の注意】の3項目。
```

### 99. 複数事業の状況を1画面で俯瞰するレイアウト案

```text
前提: 複数事業を展開しており、経営者が全体を俯瞰したい。
指示: 渡す事業リストをもとに、1画面で全事業の状況（進捗・数値）を俯瞰できるレイアウト案を設計して。
出力形式: レイアウトのブロック構成（見出しと配置の説明）。
```

### 100. エージェンティックループ化の設計メモを作る

```text
前提: 個別の自動化を1つのループにつなげる設計を検討している。
指示: 渡す既存の自動化ツール一覧（クリエイティブ生成・レポート・レギュレーションチェック等）を、どうつなげば人が介在せず回るループになるか、設計の初期メモを作って。
出力形式: 現状の自動化一覧／つなぐ順番の案／つなぐ際に検討が必要な論点。
```

---
以上、100個です。内訳: クリエイティブ生成・素材制作20本、広告運用・入稿・レポート20本、レギュレーションチェック・審査対応14本、LP分析・記事/コンテンツ生成20本、SNS企画・投稿運用15本、経営管理・ダッシュボード運用11本。